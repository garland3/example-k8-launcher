# example-k8-launcher

Demonstrator: launch short-lived AI coding agents (opencode / claude-code / codex)
as one-shot Kubernetes Jobs on OpenShift with an nginx (tinyproxy) egress sidecar
and a Postgres-backed event log.

See open PRs for the implementation.
